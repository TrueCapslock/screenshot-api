# Todos

## Release
- [ ] Bump version, commit, and push when ready for release
- [ ] Build and push Docker image for new version
- [ ] Deploy to production (Coolify)

## Dashboard
- [ ] Add i18n support to admin panel (`admin.html`)

## API
- [ ] Add webhook retry UI — allow re-sending failed webhooks for async screenshots
- [ ] Show live rate limit remaining (X-RateLimit-Remaining) rather than just configured limits
